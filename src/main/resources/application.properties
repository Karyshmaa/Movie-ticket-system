spring.application.name=moviebooking

# ===================== DATABASE =====================
spring.datasource.url=jdbc:mysql://localhost:3306/moviebooking_db
spring.datasource.username=root
spring.datasource.password=Success@1216

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===================== MAIL =====================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=karishmaaachou1228@gmail.com
spring.mail.password=wqbtqndwoiratmpw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.name=MovieBooking

# ===================== JWT =====================
app.jwt.secret=${JWT_SECRET:CHANGE_THIS_SECRET_IN_PRODUCTION_minimum_32_chars_long}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}

# ===================== RAZORPAY =====================
razorpay.key-id=${RAZORPAY_KEY_ID:}
razorpay.key-secret=${RAZORPAY_KEY_SECRET:}
razorpay.webhook-secret=${RAZORPAY_WEBHOOK_SECRET:}
razorpay.currency=${RAZORPAY_CURRENCY:INR}

# ===================== CORS =====================
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}

# ===================== FRONTEND =====================
app.frontend.url=${FRONTEND_URL:http://localhost:5173}

# ===================== CLOUDINARY =====================
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:}
cloudinary.api-key=${CLOUDINARY_API_KEY:}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:}
cloudinary.folder=${CLOUDINARY_FOLDER:cinebook/posters}

# ===================== FILE UPLOAD =====================
app.upload.dir=${UPLOAD_DIR:uploads}
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

server.port=${SERVER_PORT:8080}
